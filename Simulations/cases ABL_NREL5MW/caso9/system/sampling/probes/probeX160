probeX160
    {
          type                probes;
          functionObjectLibs ("libsampling.so");
          name                probeX160;
          outputControl       $acquisitionControl;
          outputInterval       $acquisitionInterval;
          timeStart           $acquisitionStart;
          fields
          (
              U
              T
          );

          probeLocations
          (
                 (1600.2376 1500.1 100.1)
          );
    }
